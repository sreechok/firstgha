name: Continous Deployment

on:
  push:
    branches:
      - main

jobs:
  first_jobs:
    runs-on: ubuntu-latest
    steps:
      - name: "say hello"
        run: |
          echo "deployig to master branch"
