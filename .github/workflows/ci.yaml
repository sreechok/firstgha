name: Continous Integration

on:
  pull_request:
    branches:
      - master
    types:
      - opened
      - reopened
      - synchronize

jobs:
  build:
    runs_on: ubuntu-latest
    steps:
      - name: deploy to dev
        run: |
          echo deploying to dev